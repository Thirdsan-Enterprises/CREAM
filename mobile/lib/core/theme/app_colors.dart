import 'package:flutter/material.dart';

/// Cream Restaurant & Catering Services brand palette.
/// See CLAUDE.md section 2 (Brand Identity).
class AppColors {
  AppColors._();

  static const charcoal = Color(0xFF1A1A1A);
  static const black = Color(0xFF000000);
  static const cream = Color(0xFFF7F3EC);
  static const creamDark = Color(0xFFEFE7D8);
  static const gold = Color(0xFFC9A15C);
  static const goldLight = Color(0xFFD4AF6A);

  static const white = Color(0xFFFFFFFF);

  static const success = Color(0xFF3F8A5B);
  static const danger = Color(0xFFB3432E);
}
